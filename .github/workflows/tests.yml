name: tests

on:
  push:
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6.2.0
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip 'setuptools<81' wheel
          pip install -r requirements.txt
      - name: Smoke test
        run: python experiments/smoke_test.py
      - name: Validate configs
        run: python experiments/validate_configs.py configs/mini_validation.yaml configs/cec2017_30d_probe.yaml configs/cec2017_30d_pilot.yaml
      - name: Dry-run protected main experiment
        run: python experiments/run_cec2017_main.py --dry-run
      - name: Run tests
        run: python -m pytest tests -q
